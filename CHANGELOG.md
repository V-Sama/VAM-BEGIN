# 更新日志

## V0.8.0 (2026-08-16)

# V0.8.0 更新日志

## 资源库健康工具（B）
- 资源管理新增「资源库体检」：重复包分组（归一化名+大小）、损坏包 zip 校验、游戏孤儿依赖三合一报告
- 报告窗口一键去重（每组保留一个，其余移入软回收站）、损坏包一键清理

## 备份自动化（C）
- 关闭应用时距上次备份超 7 天自动提醒，可即时增量备份（进度窗显示，完成后退出）

## 全局缺失依赖报告（D）
- 侧栏新增「缺失依赖报告」：全预设引用扫描、缺失清单 + 可得性标签（可下载/需登录/付费/需人工确认/未找到）、批量在线补齐与批量打开页面
- 与预设管理页共用同一套服务与判定口径

## 库索引加速（E）
- 资源解析改为增量式：未变文件（路径+大小+修改时间一致）直接复用预览与标签缓存，只解析新增/变更文件
- 同步/收编/卸载后的重扫从全量秒级降为增量,冷启动更顺滑

## 其它
- 设计系统落地：令牌库 + 复合组件（PagerBar/PageHeader）+ 组件预览页（F12 打开）+ AGENTS 规范
- 收藏按包身份共享、依赖目录统一 Dp 等前序修复随本版发布
- 测试 103 例全部通过
## V0.7.1 (2026-08-16)

# V0.7.1 更新日志

## 新功能
- 新增「游戏库」一级页面（侧栏资源库下方）：浏览游戏 AddonPackages 已安装包，卡片带状态徽章（已添加/手动放入），支持搜索/筛选/排序/分页/收藏/卸载（依赖树预览）；原「管理游戏包」窗口与入口移除
- 预设依赖状态三态化：✅ 在游戏中 / ❌ 游戏内无 / ❌ 缺失，判定覆盖整个游戏目录（含 Dp）
- 「检测可下载」：缺失依赖逐个到 Hub 定位并打标（可下载/需登录/付费/需人工确认/未找到）；付费判定以「Go to pay site」按钮为铁证
- 收藏按「作者.包名」身份共享：资源库与游戏库互通、跨版本保留、路径迁移不丢；旧路径式收藏自动迁移

## 修复与优化
- 依赖目录统一为 AddonPackages\Dp（VAM 只扫描 AddonPackages 递归），自动补齐/手动移动全部归一，旧 CustomDp 数据已迁移
- 修复结果面板动画崩溃与展开不完全（高度动画 NaN/HoldEnd 问题）
- 最大化后滚动/悬停卡顿：卡片位图缓存 + 快速双线性缩图
- 窗口控制图标改为 Segoe MDL2 原生字形（最小化/最大化/还原/关闭）
- 结果面板：全部/仅缺失滑轨开关与补齐按钮同行，折叠滑入滑出动效，箭头 180° 旋转
- 分页 UI 三页统一（«首页 ‹上一页 页码 下一页› 末页»）
- 侧边栏版本号同步真实版本号
- 修复收藏迁移中"集合已修改"异常等若干 bug

## 其它
- 测试 96 例全部通过
## V0.7.0 (2026-08-16)

# V0.7.0 更新日志

## 数据自保护（B）
- **收藏/标签快照与自动恢复**：元数据除资源库内主文件外，另在系统数据目录自动快照（节流写入、保留最近 10 份）；主文件丢失时启动自动从快照恢复
- **库路径自检**：资源库路径落在程序目录内时启动弹窗提醒迁移（2026-08-15 事故教训），防止升级/清理误删
- **一键全库备份**：常规设置新增「数据备份」——增量复制全部 .var 到指定磁盘并生成备份清单；距上次备份超 7 天启动提醒

## 在线下载补齐（A）
- 预设依赖页新增「在线补齐」：对缺失依赖逐个到 Virt-A-Mate Hub 自动搜索并定位资源页（打分+标题校验，避免同名场景误配）
- 配置 Hub 会话 Cookie 后可全自动下载免费包并入库校验；未配置时逐项列出「需登录/付费外部/未找到」，一键批量打开对应页面
- 常规设置新增 Hub 代理地址与会话 Cookie 配置项（默认直连、留空不下载）
- 下载限速、逐项进度与结果汇总

## 其它
- 预设依赖补齐：引用扫描覆盖全部内部路径（子场景合并/自定义根目录）、版本不匹配回退同包可用版本（V0.6.0 内容随本版一并发布）
- 测试 86 例全部通过
## V0.6.0 (2026-08-15)

# V0.6.0 更新日志

## 视觉统一
- 资源库 / 预设依赖管理 / 管理游戏包三页卡片完全统一：方形卡片、底部渐变信息栏、悬停浮起+图片放大、选中蓝框+蒙层+✓徽标
- 修复管理游戏包：选中蒙层被不透明预览图遮挡（移到图片上层）；悬停浮起放大效果丢失
- 主色统一蓝、按钮/卡片/图标角标/页面标题全局样式收编；左侧边栏入口全局化

## 预设依赖补齐增强
- 依赖引用扫描覆盖所有 var 内部路径：子场景合并（/Saves/scene/）、自定义根目录（如 /tietu/ 贴图包）不再漏扫
- 版本号不匹配时回退到同包可用版本：此前库里有同包新版却判定"缺失"、永不补齐到 CustomDp
- 引用缓存结构版本化：解析规则升级后旧缓存自动作废重扫

## 健壮性
- 部署流程固定为只复制模式（robocopy /E），数据目录与程序部署目录彻底分离，杜绝误删用户资源
- 测试 72 例全部通过；共享测试基类与 var 工厂收敛、保留目录常量集中管理（VarPaths）、AsyncVoidHelper、文件大小显示支持 TB
## V0.5.0 (2026-08-15)

# V0.5.0 更新日志

## 核心流程升级：资源统一收编

- **拖拽导入重做**：拖入 .var 文件或整个文件夹 → 自动去重入资源库 → 重建预览 → 询问是否同步到游戏库（同意走「仅同步」管线：选中→MainPak、依赖→Dp）。文件夹递归扫描、多文件夹重叠自动去重
- **启动自动收编**：嗅探游戏目录散落的未入库资源 → 移动入库（游戏目录清理）→ 自动同步回游戏库。资源无论以何种方式进入游戏库都会被纳入管理，游戏目录不再有散落文件
- **资源库「已同步」角标**：绿色角标标记已在游戏库中的资源（扫描 AddonPackages 全目录），同步/卸载后即时刷新

## 新功能

- **卸载集成资源库**：右键「卸载（从游戏移除）」/ 底部「卸载所选」批量卸载；卸载前依赖树预览（缺失标红）、自动处理孤儿依赖、本地库副本保留；管理页删除流程复用同一服务
- **全选筛选结果 + 收藏已选**：批量勾选当前筛选（跨搜索/标签累加）、取消全选、一键收藏所有已选（批量落盘）
- **预设依赖补齐升级**：预设卡片红色「缺 N」徽章；右键「查看详情」依赖窗口（缺失标红）+「补齐此预设依赖」；补齐全部依赖一键复制到 CustomDp
- **拖拽导入支持文件夹**：递归扫描其中所有 .var 批量导入

## 性能

- **预设引用解析持久化缓存**：以文件大小+修改时间失效，启动补齐与预设体检不再每次全文读取数 GB 场景文件（典型场景从 ~15 秒降到秒级）

## 修复与打磨

- 资源库首开无预览图（构造期空数据污染过滤缓存）
- 重新解析后列表顺序与「全部」筛选一致（统一走过滤管线）
- 资源库/预设页翻页自动回到顶部
- 预设页首次扫描即应用已保存的排序模式
- 资源库抽屉关闭后侧边栏高亮恢复到实际页面
- 迁移到本地同名文件移入回收站（计数器式命名去重）
- 全选与取消全选拆为两个独立按钮，语义不随筛选变化跳动
- 教程页同步新流程
## V0.4.0 (2026-08-15)

## 新功能
- 回收站升级：垃圾文件（.txt/.url）不再直接删除，改为移入回收站可恢复；整理前预览数量与大小
- 回收站保留期：可设置永久/7/30/90 天，回收站窗口显示过期文件数并支持一键清理
- 一键备份：程序配置 + 依赖清单（含本地缺失标记）打包到带时间戳的备份目录
- 导出依赖清单：一键导出 依赖清单.md，可直接分享给他人对照补依赖
- 检查更新：读取 GitHub Releases 自动比对版本，一键跳转下载页
- 空间统计卡：本地包数/总大小、回收站可回收空间、游戏 AddonPackages、作者占用 Top3
- 依赖可视化：删除前弹树形依赖预览，本地缺失依赖红色标记
- 包详情页：右键“查看详情”，展示描述/许可/依赖缺失/内容列表
- 收藏 / 最近使用 / 自定义标签：卡片金色星标、筛选栏一键过滤、右键菜单管理，本地持久化
- 预览缓存面板：显示缓存占用/文件数/上限，支持一键清理
- 整理结果报告卡片：整理后展示 7 项指标统计、错误明细，可打开整理日志

## 体验优化
- 备份与导出清单带实时进度（正在备份... i/n）
- “最近使用”限 30 天窗口并按时间倒序；“全部”按钮一键重置所有筛选
- 整理/迁移/备份后空间统计自动刷新

## 修复
- 修复依赖环可能导致无限递归崩溃
- 修复包名含点号时依赖被误判为本地缺失
- 修复整理同名文件被静默覆盖（改为自动改名避让）
- 修复预览缓存清理误删索引文件
- 修复收藏筛选缓存不刷新、批量操作逐文件写盘导致界面卡顿
- 回收站/Others 路径判断改为精确边界匹配，杜绝子串误伤
- 配置保存改为临时文件原子替换，防止意外损坏
- 缓存清理并发竞态修复；依赖注入包版本对齐 net8.0

## 性能
- 预览缓存保存由 3 次序列化降为 1 次（字符串去重），限容索引一遍读取，大小统计节流
- 备份/导出依赖清单改为并行读取（复用解析并行度），大库耗时大幅下降
- 磁盘统计 30 秒结果缓存，页面切换不再反复扫盘

## 工程化
- 新增单元/集成测试 35 例（dotnet test），全部通过
- 新增 GitHub Actions 构建+测试流水线与 .editorconfig
## V0.3.0 (2026-08-14)

- 架构：引入 Microsoft.Extensions.DependencyInjection，页面与服务分层（设置/主题/弹窗/Var 文件服务）
- 新增：预设管理器（场景/外观/姿势预设的浏览与管理）
- 修复：依赖解析遇到循环依赖时可能无限递归导致崩溃
- 修复：二次启动时互斥体释放异常
- 修复：整理 Var 包时同名文件被静默覆盖（现自动改名避让）
- 修复：预览缓存清理误删索引文件（preview_data_* / tagcounts_cache_*）
- 修复：删除/迁移/解析大量包时界面卡顿（磁盘与 zip 读取移入后台线程）
- 修复：包名含多段点号时版本号解析错误
- 优化：解析并行度自动钳制到合理范围，避免配置误伤性能
- 优化：所有空异常捕获补充日志，问题可追溯
- 优化：首页订阅设置变更自动刷新，窗口卸载时正确退订
- 其他：构建产物不再纳入版本控制，仓库历史大幅瘦身
