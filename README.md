# VAM-BEGIN

<p align="center">
  <img src="images/VB2.ico" width="96" alt="VAM-BEGIN Logo"/>
</p>

<p align="center">
  <b>一款简洁高效的 Virt-A-Mate 资源包管理工具</b>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/Platform-Windows%2010%20%2F%2011-0078d4?logo=windows" alt="Windows"/>
  <img src="https://img.shields.io/badge/Runtime-.NET%208.0-blue?logo=dotnet" alt=".NET 8.0"/>
</p>

---

## ✨ 功能一览

- 🎮 **一键启动游戏** — 支持桌面模式 / VR 模式 (OpenVR)
- 🖱 **拖拽导入** — 把下载好的 .var 文件直接拖进窗口，自动导入本地库
- 📋 **选择 Var 包** — 预览图浏览、搜索、分类筛选、⭐ 收藏 / 🕒 最近使用 / 🏷 自定义标签；自动解析依赖并按需复制到游戏
- 🌲 **依赖可视化** — 删除前展示依赖树预览，本地缺失依赖红色标记
- 📦 **包详情页** — 描述 / 许可 / 依赖缺失 / 内容列表一目了然
- 📦 **迁移 Var 包** — 从游戏目录批量迁移到本地管理路径，释放游戏目录空间
- 🔍 **解析 Var 包** — 提取预览图并缓存，加速后续浏览
- 📂 **整理 Var 包** — 按作者归类、智能去重、垃圾文件移入回收站（整理前预览、完成后报告卡片）
- ♻ **回收站** — 保留期设置（7/30/90 天）、过期一键清理、选择性删除
- 💾 **一键备份 / 导出依赖清单** — 配置备份 + 依赖清单（含本地缺失标记），可导出分享
- 📊 **空间统计** — 扇形图占比（本地库/回收站/游戏目录）+ 明细与作者 Top3
- 🧹 **预览缓存面板** — 占用/上限展示，一键清理
- 🔄 **检查更新** — 一键检查 GitHub 新版本并跳转下载
- 🎨 **个性化** — 浅色/深色/跟随系统主题，自定义壁纸

---

## 🚀 快速开始

### 环境要求

- Windows 10 / 11
- [.NET 8.0 Desktop Runtime](https://dotnet.microsoft.com/download/dotnet/8.0)

### 安装与使用

1. 从 [Releases](../../releases) 下载最新版本
2. 解压后运行 `VamBegin.exe`
3. 点击左上角箭头按钮打开侧边栏，进入 **设置 → 路径**
4. 配置以下路径：
   - **游戏安装路径** — Virt-A-Mate 游戏根目录
   - **本地 Var 包路径** — 存放 .var 资源包的目录（不设置则使用程序目录下的 AddonPackages）

### 推荐流程

```
最简用法：设置路径 → 把 .var 拖进窗口 → 选择 Var 包 → 启动游戏
大库管理：迁移 Var 包 → 解析 Var 包 → 整理 Var 包 → 回收站
```

---

## 📖 功能详解

### 🎮 首页 · 启动游戏

提供 **桌面模式** 和 **VR 模式** 两个启动按钮，点击即可启动 Virt-A-Mate。

> 💡 点击首页的 "VAM-BEGIN" 标题可以随机切换壁纸 🎲
> 💡 直接把 .var 文件拖进窗口即可导入本地库。

### 🖱 拖拽导入

- 从资源管理器把 .var 文件拖进主窗口，松开即导入本地库
- 同名文件自动跳过，导入结果实时显示

### 📋 选择 Var 包

核心功能，实现 **按需加载**：

- 浏览本地所有 .var 资源包，支持预览图、搜索、分类筛选
- ⭐ 收藏 / 🕒 最近使用（30 天内）过滤，🏷 自定义标签
- 右键卡片：收藏 / 添加自定义标签 / 查看详情 / 在 Hub 搜索 / 打开所在文件夹
- 勾选需要的资源包后，程序自动分析依赖关系并复制到游戏目录
- 删除前展示依赖树预览，本地缺失的依赖红色标记
- 默认开启「仅添加」模式 — 增量复制，不清除已有资源

### 📦 包详情页

右键「查看详情」查看单个包的完整信息：描述、许可、依赖列表（缺失标红）、内容文件。

### 📦 迁移 Var 包

将游戏目录 `AddonPackages` 下的 .var 文件批量迁移（剪切）到本地 Var 包路径。释放游戏目录空间，配合「选择 Var 包」实现按需加载。

### 🔍 解析 Var 包

扫描本地 .var 文件，提取预览图并建立缓存。首次使用前建议先执行一次解析，后续浏览将直接从缓存加载，大幅提升速度。

### 📂 整理 Var 包

对本地 Var 包目录进行自动整理：

| 操作 | 说明 |
|------|------|
| 按作者归类 | 自动创建作者子目录，将文件归入对应文件夹 |
| 智能去重 | 保留最新版本，旧版本移入回收站 |
| 非标准文件 | 命名不规范的文件移入 Others 目录 |
| 清理冗余 | .txt / .url 移入回收站（可恢复），清理空文件夹 |

整理前会预览待回收文件，完成后弹出报告卡片（统计 + 错误明细 + 打开日志）。

### ♻ 回收站

管理「整理 Var 包」产生的重复/无效文件：

- 查看文件列表和占用空间
- 保留期设置：永久 / 7 / 30 / 90 天（设置 → 路径）
- 一键清理超过保留期的文件
- 支持勾选删除或一键清空

### 💾 备份与导出

- **一键备份**：程序配置 + 依赖清单（含本地缺失标记）打包到带时间戳目录，实时进度
- **导出依赖清单**：生成 `依赖清单.md`，可直接分享给他人对照补依赖

### 📊 空间统计

本地库 / 回收站 / 游戏目录的包数与占用以扇形图直观展示（悬停显示百分比），并列出作者占用 Top3 与可回收空间。

### 🧹 预览缓存

显示预览图缓存占用与上限（默认 500MB，超限自动淘汰最旧文件），支持一键清理。

### 🔄 检查更新

设置 → 路径页点击「检查更新」，自动比对 GitHub 最新版本，一键跳转下载页。

### ⚙ 设置

| 设置项 | 说明 |
|--------|------|
| 📂 路径 | 配置游戏安装路径和本地 Var 包路径，修改后自动保存 |
| ♻ 回收站保留期 | 永久 / 7 / 30 / 90 天 |
| 🔄 版本与更新 | 检查新版本并跳转下载页 |
| 🖼 壁纸 | 管理背景壁纸列表，支持添加自定义图片，启动时随机展示 |
| 🎨 主题 | 浅色 / 深色 / 跟随系统 三种模式 |

---

## ❓ 常见问题

**Q: 启动时提示"游戏路径未配置"？**
> 打开侧边栏 → 设置 → 路径，配置 Virt-A-Mate 游戏的根目录。

**Q: 选择 Var 包后游戏里看不到资源？**
> 确保游戏目录 `AddonPackages` 根目录下没有散落的 .var 文件。如果有，建议先使用「迁移 Var 包」功能将它们迁移到本地路径。

**Q: 预览图加载很慢？**
> 首次使用前先执行一次「解析 Var 包」，建立预览图缓存后浏览速度会大幅提升。

**Q: 整理后文件丢失了？**
> 整理操作不会直接删除任何 .var 文件，去重/冗余文件会移入回收站（`VAMB_RecycleStation` 目录），可随时查看和恢复。可在设置中配置回收站保留期。

**Q: 如何备份配置和依赖清单？**
> Var 管理页 →「一键备份」（配置 + 依赖清单打包）或「导出依赖清单」（单独生成 `依赖清单.md`）。

**Q: 如何检查新版本？**
> 设置 → 路径页 →「检查更新」，会自动比对 GitHub 最新 Release 并跳转下载页。

---
